//! ph0b0s CLI entrypoint. Wires LLM adapter, detector registry, store, and
//! reporter, then dispatches subcommands. Implementation in a follow-up pass.

fn main() -> anyhow::Result<()> {
    eprintln!("ph0b0s: scaffold-only build; CLI not yet implemented");
    Ok(())
}
